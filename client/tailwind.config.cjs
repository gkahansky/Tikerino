/**
 * The design tokens are used verbatim. This file is a pointer, not a copy, so the
 * Tailwind theme and specs/tikerino-design-tokens-v1.css can never drift apart.
 */
module.exports = require('../specs/tikerino-tailwind.config.js');
